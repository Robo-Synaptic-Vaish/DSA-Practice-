// selection sort in c

#include <stdio.h>

void selectionSort(int arr[], int n)
{
    for (int i = 0; i < n - 1; i++)
    {
        int minIndex = i;  // assume current position holds the minimum 
        //.....to be continued
    }
}